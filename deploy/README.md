# Deploy

| Path | Description |
|------|-------------|
| [kubernetes/](kubernetes/) | Kubernetes Job, ConfigMap, Secret example for running TexWeave once |

Docker: see root [Dockerfile](../Dockerfile) and [docker-compose.yml](../docker-compose.yml).
